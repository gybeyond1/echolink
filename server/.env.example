# EchoLink 服务端配置样例
# 复制本文件为 .env 后按需修改；.env 本身不入库（已被 .gitignore 忽略）。
# 监听端口
PORT=4000
# JWT 密钥：首次启动会自动生成并持久化到数据目录 .jwt_secret，一般无需手动设置。
# 如需固定密钥，请设置一个长度 >= 32 的随机字符串（变量名 JWT_SECRET）。
# SQLite 数据库文件路径
DB_PATH=./data/notifysync.db
# 允许保留的通知/消息历史条数
MAX_NOTIFICATION_HISTORY=200
